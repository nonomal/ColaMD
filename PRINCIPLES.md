# ColaMD Principles

产品与工程上的判断标准。规范的细节在各自的文件里，这里记录**为什么这样定**，以及踩过的坑。

## 适用范围

这份文档对**所有参与者**生效：维护者、外部贡献者，以及在本仓库里干活的 Agent。动手之前先读 [AGENT.md](AGENT.md) 与本文档；提交界面改动前还要过一遍 [design.md](design.md) 的贡献检查清单。

与这些原则冲突的方案会被拒绝或要求先改规范，理由会写进 [docs/feature-requests.md](docs/feature-requests.md)。其中「如非必要，勿增实体」是默认立场：新增任何 UI 元素、依赖或子系统，都需要先证明必要性。

相关文档：[AGENT.md](AGENT.md)（产品定位与分工）· [design.md](design.md)（界面规范）· [docs/feature-requests.md](docs/feature-requests.md)（需求裁决记录）

## 1. 稳定性优先

用户最不能接受的是一次数据丢失或一次卡死。所以：

- **修复优先于新功能**：队列里有 bug 时先修，再谈新功能
- **数据丢失类问题优先级最高**：撤销把上一篇文章的内容带回当前文档、自动保存覆盖外部改动，这类问题排在最前面
- 修复版不必等周节奏，可以尽快发；功能仍按周批次集中发

## 2. 用户数据不可丢

- 程序化的内容替换（打开文件、新建文件、外部热更新、冲突后加载磁盘版本）**不得进入用户的撤销栈**，否则撤销会把另一篇文档的内容写回当前文件，随后被自动保存落盘
- 自动保存**不得覆盖外部写入**：检测到外部改动且本地有未保存内容时，先让用户选择
- 切换文件必须重置编辑器状态，不残留上一篇的选区、搜索、滚动位置与撤销历史

## 3. 先测量，再优化

性能问题先量化再动手，没有测量的优化是猜测。Windows 启动速度（#32）与打字卡顿（#78）都先拿数据、再决定改哪里。

## 4. 不猜，也不夸大

- 功能描述必须与实际行为一致，做不到的就写清楚做不到
- 修复声明要经过真机验证，再对外说「已修好」
- 用户跑着的那个版本才算数：改动没进 Release，就不算对用户生效

## 5. 如非必要，勿增实体

界面只有标题栏、编辑器、文件列表。每增加一个元素先问：解决了什么明确问题？能否用已有控件完成？是否增加长期视觉噪音？默认答案是否。

据此拒绝过的方案：可配置图片导入管道（#73）、飞书风格图片工具栏（#74）、超宽屏双页排版（#67）、Agent 活动指示器（已删除）。理由见 [docs/feature-requests.md](docs/feature-requests.md) 的 Declined 区。

## 6. 决策要留痕

每个被接受或被拒绝的需求都写进 [docs/feature-requests.md](docs/feature-requests.md)，并写清理由，避免同一个方案被反复提出、反复讨论。

## 7. 先改规范，再写实现

新需求与 `design.md` 冲突时，先更新规范并说明理由，再动代码。规范与实现不一致时，以规范为准。

## 8. 工具链改动：结论与动作一起落地

改 CI、依赖或打包配置时，结论必须和动作一起完成，并在本地验证。踩过的坑：解钉 macOS runner 时没有同步升级 electron-builder，导致连续两轮发版卡在签名环节。

## 9. 对外表述

- 文案不使用破折号（`—`）：列表项用冒号，正文用逗号、冒号或括号
- 定位讲用户价值（免费、优雅、简单、文件永远是最新的），不用「AI Agent 原生」自我定义；Agent 只作为实时同步的一个举例
- 官网首页功能卡片固定 9 张，主题区排在功能卡片之前；完整功能列表只写在 README
- 每个版本在 `resources/demo/changelog.md` 追加本版内容，演示目录是「可玩的 changelog」
