# 大纲功能验收手册

> 本文档用于验收大纲的四项更新（issue #64）。打开侧栏「大纲」标签页，按下面的清单逐项体验。

## 验收清单

1. **阅读进度同步**：用滚轮随意滚动正文，大纲中当前章节应实时变为主题色并自动保持在可视范围内；滚到文档最底部时应激活最后一项。
2. **大纲点击跳转 + 闪烁**：点击大纲任意条目，正文平滑滚动到位后，落点标题整行会闪过一次主题色高亮带（约 1.6 秒渐隐）。
3. **文内链接跳转 + 闪烁**：点击正文中「跳转演示」一节里的文内链接，同样会跳转并闪烁落点标题。
4. **长标题悬停提示**：找到「2.1」那条超长标题，大纲中悬停可看到完整标题的 tooltip。
5. **多主题**：菜单「主题」切换深色主题（如 Nord、Dracula），以上颜色应自动适配，无突兀。

---

## 一、基础层级

这是二级标题下的正文，用于观察滚动时的进度高亮。

### 1.1 三级标题 A

继续向下滚动，大纲高亮应当从上一项切换到当前项。

### 1.2 三级标题 B

大纲中的当前项应保持主题色，并自动露出在可视范围内。

#### 1.2.1 四级标题嵌套

更深层级的标题也参与进度高亮，缩进与大纲层级一致。

## 二、超长标题的显示与提示

### 2.1 这是一个刻意写得很长的标题用于验证侧栏宽度固定时条目截断以及悬停提示是否完整显示整个标题文本

这一条在大纲中会以省略号截断；鼠标悬停应出现 tooltip，展示完整标题文字。

### 2.2 短标题对照

正常长度标题的行为不应受到影响。

## 三、跳转演示

点击下面的文内链接，正文应跳转到对应标题并闪一次主题色高亮带：

- [跳到「重复的标题名」第一处](#重复的标题名)
- [跳到「重复的标题名」第二处](#重复的标题名-1)
- [跳到文末的「六、文档末尾」](#六文档末尾)

跳转后注意观察：大纲高亮应直接落在目标章节，且不会被中途的其他章节抢走。

## 四、重复标题与特殊字符

### 重复的标题名

第一个同名标题（上文第一条链接的落点）。

### 重复的标题名

第二个同名标题（上文第二条链接的落点），两次跳转应各自落到正确位置。

### 带 `代码`、**加粗** 和 [外部链接](https://example.com) 的标题

标题内的行内标记不应影响大纲条目文字与跳转定位；外部链接仍需 ⌘/Ctrl+点击 才会在浏览器打开。

## 五、大量标题的跟随滚动

### 5.1 项

滚动区间占位。

### 5.2 项

滚动区间占位。

### 5.3 项

滚动区间占位。

### 5.4 项

滚动区间占位。

### 5.5 项

滚动区间占位。

### 5.6 项

滚动区间占位。

### 5.7 项

滚动区间占位。

### 5.8 项

滚动区间占位。

### 5.9 项

滚动区间占位。

### 5.10 项

滚动区间占位。

### 5.11 项

滚动区间占位。

### 5.12 项

滚动区间占位。

## 六、文档末尾

滚动到页面最底部时，大纲应激活最后一项。

回到顶部时，高亮应回到第一个标题，不应残留在文档中部。
