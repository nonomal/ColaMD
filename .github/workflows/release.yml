name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      mac_arch:
        description: 'macOS architecture to build'
        type: choice
        options:
          - universal
          - x64
          - arm64
        default: universal

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: mac
            sign: true
          - os: windows-latest
            platform: win
          - os: ubuntu-latest
            platform: linux

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install

      - run: npm run build

      - name: Build distributables
        shell: bash
        run: |
          ARCH_ARGS=""
          if [ "${{ matrix.platform }}" = "mac" ]; then
            case "${{ inputs.mac_arch || 'universal' }}" in
              x64) ARCH_ARGS="--x64" ;;
              arm64) ARCH_ARGS="--arm64" ;;
              universal) ARCH_ARGS="--universal" ;;
              *) ARCH_ARGS="--universal" ;;
            esac
          fi
          npx electron-builder --${{ matrix.platform }} $ARCH_ARGS --publish never
        env:
          CSC_LINK: ${{ matrix.sign && secrets.CSC_LINK || '' }}
          CSC_KEY_PASSWORD: ${{ matrix.sign && secrets.CSC_KEY_PASSWORD || '' }}
          APPLE_ID: ${{ matrix.sign && secrets.APPLE_ID || '' }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ matrix.sign && secrets.APPLE_APP_SPECIFIC_PASSWORD || '' }}
          APPLE_TEAM_ID: ${{ matrix.sign && secrets.APPLE_TEAM_ID || '' }}

      - uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.platform }}
          path: |
            release/*.dmg
            release/*.zip
            release/*.exe
            release/*.AppImage
            release/*.deb
            release/*.yml
            release/*.blockmap
          if-no-files-found: ignore

  # Tag pushes publish a release; manual dispatches are build-only so the
  # artifacts can be verified (or attached manually) before publishing.
  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
